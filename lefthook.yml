pre-commit:
  commands:
    format:
      glob: "**/*.{ts,tsx}"
      run: bun run prettier --write --ignore-unknown {staged_files}

    lint:
      glob: "**/*.{ts,tsx}"
      run: bun run eslint --fix {staged_files}
